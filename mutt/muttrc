# Folder hooks
folder-hook 'account.yandex' 'source ~/.mutt/account.yandex'

# Default account
source ~/.mutt/account.yandex

# Fetch mail shortcut
bind index G imap-fetch-mail
set imap_keepalive = 900

set editor = "vim"
set imap_check_subscribed
set mail_check = 120

set sort=threads
set sort_aux = reverse-last-date-received  # like gmail
set sort_aux = reverse-last-date-received  # like gmailil set date_format = "%m/%d/%t" = "+[GMail]/Drafts"

# Side bar
set sidebar_width=24
set sidebar_visible=yes
bind index \CP sidebar-prev
bind index \CN sidebar-next
bind index \CO sidebar-open
bind pager \CP sidebar-prev
bind pager \CN sidebar-next
bind pager \CO sidebar-open

# Donedebar-open
set sidebar_width=24
set sidebar_visible=yes
<<<<<<< HEAD
# set sidebar_delim='|'	
=======
# set sidebar_delim='|'

source ~/.dotfiles/mutt/dracula.muttrc

#	
>>>>>>> 30efaf08b504fa7f758b10cdeef9e39f6da6d474
